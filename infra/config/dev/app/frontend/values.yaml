# Dev frontend - only dev-specific overrides

namespace: dev

# API configuration for dev
api:
  baseUrl: "http://backend.dev.svc.cluster.local:8000"

# Development features
features:
  hotReload: true
  sourceMap: true