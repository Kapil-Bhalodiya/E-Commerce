# Loki log aggregation system configuration for development
# SingleBinary deployment for local development and testing

loki:
  # Disable distributed components
  backend:
    replicas: 0
  read:
    replicas: 0
  write:
    replicas: 0