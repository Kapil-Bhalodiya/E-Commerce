# release:
#   revision: main
#   tag: HEAD
# project: default
# address: https://kubernetes.default.svc
global:
  env: dev
target:
  address: https://kubernetes.default.svc
  namespace: dev
  project: default
loki:
  storage:
    bucketNames:
      chunks: ""
      ruler: ""
      admin: ""