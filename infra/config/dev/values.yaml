# release:
#   revision: main
#   tag: HEAD
# project: default
# address: https://kubernetes.default.svc
global:
  env: dev
target:
  address: https://kubernetes.default.svc
  namespace: dev
  project: default
  branch: Dev-Metrics-Alternative-Monitoring-Approaches
loki:
  storage:
    bucketNames:
      chunks: ""
      ruler: ""
      admin: ""