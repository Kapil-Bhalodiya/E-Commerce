# replicaCount: 3
# image:
#   tag: "1.0.0"
# metrics:
#   enabled: true
# tracing:
#   enabled: true
# slo:
#   enabled: false


replicaCount: 1
resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"
serviceMonitor:
  enabled: false