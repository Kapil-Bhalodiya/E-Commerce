{{- if .Values.routes.frontend.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  namespace: {{ .Values.routes.frontend.service.namespace }}
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Values.routes.frontend.hostname }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.routes.frontend.service.name }}
            port:
              number: {{ .Values.routes.frontend.service.port }}
{{- end }}