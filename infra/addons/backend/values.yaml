replicaCount: 1
namespace: dev

image:
  repository: kapilbhalodiya/ecom-backend
  tag: "15-5d7ee81"
  pullPolicy: Always

env:
  MONGODB_URI: mongodb+srv://kapil001:UspGS7dAe8lxbEVQ@cluster0.lzfp5mu.mongodb.net
  NODE_ENV: "production"

service:
  type: ClusterIP
  port: 8000

metrics:
  enabled: true
  path: /metrics
  port: 8000

tracing:
  enabled: true
  jaegerAgentHost: jaeger-agent
  jaegerAgentPort: 6831

slo:
  enabled: false
  objectives:
    availability: 99.9
    latency: 0.5