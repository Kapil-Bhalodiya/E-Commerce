apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace:  {{ .Values.namespace }}
type: Opaque
data:
  mongodb-root-username: {{ .Values.mongodb.username | b64enc }}
  mongodb-root-password: {{ "password" | b64enc }}  # Change this in production