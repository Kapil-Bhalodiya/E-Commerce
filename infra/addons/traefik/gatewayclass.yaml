# GatewayClass defines the class of Gateway that can be instantiated
# This is a cluster-scoped resource that defines which controller will manage Gateways of this class
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: traefik  # Name of the GatewayClass - referenced by Gateway resources
spec:
  # Controller that will manage Gateways of this class
  # traefik Gateway controller handles the actual traffic routing and load balancing
  controllerName: traefik.io/gatewayclass-controller